# M13 — Final Production Requirements — Gap Audit

> Audits `docs/gates/m13-staging-evidence.md` and `docs/m13-production-deployment-plan.md` against the 20 Final Production Requirements. For each: what already holds (with evidence), what was a gap (now closed in the rewritten deployment plan, `docs/m13-production-deployment-plan.md` v2), and what remains a genuine decision only the product owner can lock. No production system was touched to produce this audit.

| # | Requirement | Status before this audit | Resolution |
|---|---|---|---|
| 1 | Production Identity Guard by **ref**, not name; staging ref must never be mistaken for production | Partial — plan checked ref after linking, but didn't hardcode a blocklist of known-wrong refs | **Closed** — plan v2 §2 now checks the linked ref against an explicit allowlist-of-one AND an explicit blocklist (staging ref `bugqhoktzzdvduzfjctm`, the unrelated third project's ref) before every mutating step, not just once after linking |
| 2 | Backup & DR Gate — PITR or alternative + RPO/RTO + risk acceptance, no pretending backups exist | Gap — flagged `pitr_enabled: false` but no RPO/RTO or risk-acceptance structure | **Partially closed** — plan v2 §0.1 now has a structured Backup/DR Decision Record with RPO/RTO fields and an explicit risk-acceptance sign-off block. **The decision itself (enable PITR vs. accept the risk) still requires the product owner** — see the lock questions below |
| 3 | Production Seed Policy — locked allowlist, no sample/demo/test/synthetic data | Gap — mentioned seed.sql as "the decision" but never enumerated an allowlist/blocklist or verified seed.sql's actual contents | **Closed** — verified factually (not asserted): `supabase/seed.sql` contains exactly 3 teams, 24 players, 3 matches, 1 Hall of Fame row, 1 tournament_settings update — zero references to `sample`, `demo`, `test`, `fake`, or the M13 staging test account. Locked allowlist/blocklist written into plan v2 §3 |
| 4 | Legacy Data Integrity — zero silent loss, exceptions go to the registry | Present but not emphasized as a hard stop | **Closed** — plan v2 §5 states parity-check failure is a hard stop, not a warning; cross-references `docs/migration/r25-approved-deprecation-registry.md` for anything that can't migrate |
| 5 | Team T/X lost assets — no fabrication, graceful degradation, replaceable later | Already locked in M12.5 (`SOURCE_LOST_PENDING_REPLACEMENT`), restated here | **Confirmed carried forward, not reopened** — no gallery display page exists yet (confirmed in M12.5), so "graceful degradation" is a requirement recorded for whenever that page is built, not a current gap. Re-confirm in the lock questions that no replacement photos have arrived since M12.5 |
| 6 | Auth/RBAC/RLS Production Gate post-deploy | Present narratively (§8) | **Closed** — plan v2 §7 is now an explicit checklist matching the exact chain: signup → profile trigger → login → JWT → role → admin authorization → admin write → audit log, plus anon/user/admin RLS matrix |
| 7 | Storage Production Verification — not just "upload succeeded" | Present (§5) but didn't explicitly require a real fetch/render check | **Closed** — plan v2 §6 requires an actual `curl`/browser fetch of at least one real object per bucket type, not just an upload success count |
| 8 | Realtime Production Verification with revert | **Missing entirely from the plan** | **Closed** — plan v2 §8 is a new, explicit step: controlled mutation → confirm event received with correct payload → revert → confirm reverted state persisted |
| 9 | Secrets Governance — never commit/echo/log; scanner PASS before AND after | Present narratively | **Closed** — plan v2 §1 (Preflight Gate) requires a secret scan pass *before* any mutation; §10 requires one *after* deployment too |
| 10 | Preflight Gate — git state, ref, migration history, env vars, secret scan, backup readiness, rollback prerequisites, ALL before mutation | Weak — only had ref + backup checks | **Closed** — plan v2 §1 is now a 7-point mandatory checklist; every item must show PASS before step 2 (linking) even begins |
| 11 | Deterministic migrations — no manual "quick fixes" outside the repo | Implicit | **Closed** — plan v2 §4 states this as an explicit rule with a concrete alternative (author a new migration file, commit it, deploy it through the same path) |
| 12 | Rollback as a requirement — trigger conditions, exact commands, DB + app recovery, post-rollback verification; non-rollback-able destructive migrations must be fixed pre-GO | Present but narrative, no explicit trigger-condition table | **Closed** — plan v2 "Rollback Plan" section rewritten as an explicit trigger-condition table + exact commands per scenario + a post-rollback verification checklist. Also added a migration-destructiveness review — **an earlier draft of this audit incorrectly claimed all 11 migrations were non-destructive; grepping every file found 2 that are not** (`20260804000004`/`20260804000006`, both `DROP COLUMN`). Both are safe only because this is a first-time deployment to an empty project (column created and dropped within the same batch, before any real row exists) — corrected and explained accurately in plan v2's Rollback Plan table, with an explicit rule that this reasoning does not extend to any future migration once production has real data |
| 13 | Post-Deployment Smoke Test | **Missing entirely** | **Closed** — plan v2 §9, explicit checklist: homepage, nav, teams, players, matches, standings, predictions, auth, admin critical paths, assets, API/DB connectivity |
| 14 | Full Production Certification Gate — safe-before vs safe-after, no reckless destructive suites on real data | Not categorized | **Closed** — plan v2 §10 explicitly splits gates into pre-deploy-safe (typecheck/lint/build), post-deploy-safe-read-only (accessibility, data parity, RLS read checks), and post-deploy-safe-with-immediate-revert (the one Realtime/write probe, cleaned up immediately) — and explicitly forbids running the local `live-data.spec.ts`/write-heavy suites against production as-is (they'd leave test artifacts in real data) |
| 15 | Observability — check Supabase/Postgres/API/Auth/Storage/Realtime logs + app errors, not just "homepage loads" | **Missing entirely** | **Closed** — plan v2 §11, explicit log-check list per service |
| 16 | Automatic STOP/rollback conditions | Not enumerated | **Closed** — plan v2 §12, explicit list: ref mismatch, migration failure, parity mismatch, RLS/security regression, critical E2E failure, Auth/RBAC failure, asset corruption, secret exposure |
| 17 | Human Production Gate — bypass permissions ≠ deploy authorization | Stated | **Reaffirmed, made more prominent** — plan v2 top banner and §13 restate this explicitly |
| 18 | Staging persists after go-live | Not stated | **Closed** — plan v2 §14: staging is never deleted/downgraded post-GO; it becomes the standing pre-release environment for every future change |
| 19 | Architecture Freeze still applies — no microservices, no stack change, no large refactor absent a real technical blocker | Not stated | **Closed** — plan v2 §15, explicit reaffirmation |
| 20 | Final artifacts — staging evidence + deployment plan reconciled pre-GO; a Production Certification Report produced post-deploy with commit SHA, migration state, identity verification, gates, exceptions, rollback readiness, PASS/FAIL | Two files existed; no certification report template | **Closed** — this audit reconciles the two existing files; `docs/m13-production-certification-report-TEMPLATE.md` created as the exact structure to fill in for real only after actual production deployment (currently all fields say PENDING — not fabricated) |

## What this audit does NOT do

- Does not touch production or staging in any mutating way (staging's known-good state from the prior phase is unchanged).
- Does not itself authorize a "Production One-Shot Directive" — per the stated sequencing, that is a separate, subsequent step.

## All 4 decisions — now LOCKED

| Decision | Locked answer |
|---|---|
| Production ref | ~~`wmamuqylqqikvseuqerm`~~ — **REVOKED** (belonged to a different, unrelated project; now permanently denylisted). **Re-locked: `tqdsvtuspehavhcygipf`** (`ptx-summer-cup-2026-production`) — a newly-created, dedicated project, confirmed by the product owner. See `docs/m13-production-deployment-blocked-incident.md` for the full incident record and `docs/m13-production-deployment-plan.md` §0.1 for the current lock. |
| Backup/DR strategy | Alternative strategy (no PITR): daily automated dump + mandatory pre-deployment/pre-migration dump + mandatory pre-bulk-admin-change dump. RPO ≤24h normal / ≈0 for controlled changes. RTO ≤2h. Integrity-checked, stored outside the production DB, periodically restore-tested. Deployment is blocked if the pre-deployment backup or its verification fails. Risk formally accepted by the product owner. |
| Production Seed Policy | Accepted as-is — the allowlist/blocklist verified against the real `seed.sql` in this audit |
| Team T/X lost assets | No change from M12.5 — still `SOURCE_LOST_PENDING_REPLACEMENT`, launch without them |

See `docs/m13-production-deployment-plan.md` §0 for the full locked record with rationale. With all 4 locked, the plan is ready for a Production One-Shot Directive whenever the product owner chooses to issue one — this audit does not issue that directive itself.
