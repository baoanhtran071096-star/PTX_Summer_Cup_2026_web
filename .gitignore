# Secrets — KHÔNG BAO GIỜ commit file .env thật
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db

# Cấu hình Claude Code riêng của từng máy. launch.json thì dùng chung được (nó mô tả
# cách chạy server xem trước), còn settings.local.json là quyền/tuỳ chọn của riêng máy đó
# — đẩy lên repo chung sẽ ghi đè lựa chọn của máy kia.
.claude/settings.local.json

# Firebase & build
.firebase/

# Logs
*.log
